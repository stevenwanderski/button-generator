(function($){
    $.fn.bxHoverScroll = function(options){
		
		var defaults = {
			
		};
		
		var o = $.extend(defaults, options);
		
		return this.each(function(){
			var $this = $(this);
			
		});
		
	}
		
})(jQuery);